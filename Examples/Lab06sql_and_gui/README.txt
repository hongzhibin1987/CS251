
Here we demonstrate a very basic, partial integration of SQL and GUI using the employees database.

Click Button 1 to run a select query and see the results displayed in the TextArea on the bottom of the GUI.

Note that TextFields are only single line, but TextAreas are multiline.

Also shown is how to change the font size and color
